# 協作開發設定說明

> ✅ **新做法（推薦）：改用「共用雲端資料庫」**
> 只要大家的 `.env` 都指向同一個 Azure SQL Database，資料就是即時共用的——
> 誰新增課程／使用者，其他人重新整理網頁就看得到，**再也不用 `dumpdata`／`loaddata`**。
> 設定方式見下方〈零、改用共用雲端資料庫〉。
>
> ⚠️ 舊做法（本機各自裝資料庫 + fixtures 匯入）仍保留在下面當備援，
> 但只要設好 `.env`，就不需要再走匯出／匯入那一套了。
>
> 專案已從 MySQL 遷移到 Azure SQL Database（微軟 SQL Server），完整部署步驟見
> [AZURE_DEPLOYMENT.md](AZURE_DEPLOYMENT.md)。

---

## 零、改用共用雲端資料庫（設定一次，之後免同步資料）

1. **負責人**在 Azure Portal 建立一個 **Azure SQL Database**（學生可用 Azure for
   Students 免費額度），拿到 `伺服器名稱.database.windows.net / database / user / password`，
   並在防火牆規則允許組員的 IP。
2. 每位組員在專案根目錄把範本複製成 `.env`：
   ```bash
   # Windows PowerShell
   Copy-Item .env.example .env
   # Mac / Linux
   cp .env.example .env
   ```
3. 打開 `.env`，填入負責人給的那組**同一份**連線資訊（大家填一樣的）。
4. 安裝 **ODBC Driver 18 for SQL Server**（[下載頁](https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server)，
   Windows 版是安裝檔，Mac 用 `brew install msodbcsql18`）。
5. 安裝套件並建表：
   ```bash
   pip install -r requirements.txt
   cd myproject
   python manage.py migrate      # 只有第一次、或有人新增 migration 時要跑
   python manage.py runserver
   ```

設好之後，日常協作就只剩：**改程式 → `git pull` / `git push`**。
資料在雲端共用，不必再匯出匯入。`.env` 已被 git 忽略，密碼不會外流。

---

## 一、（備援）不想連共用雲端，改用本機 SQL Server

優先建議直接照上面〈零〉設定共用雲端資料庫。真的需要離線開發時，可用 Docker
在本機跑一個 SQL Server：

```bash
# 1. 取得專案
git clone https://github.com/justingywen/NTGGWP.git
cd NTGGWP

# 2. 安裝套件
pip install -r requirements.txt

# 3. 本機起一個 SQL Server（Docker）
docker run -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=YourStrong!Passw0rd" ^
  -p 1433:1433 --name sql-local -d mcr.microsoft.com/mssql/server:2022-latest

# 4. 建資料庫（用 sqlcmd 或 Azure Data Studio 連上去執行）
CREATE DATABASE course_platform_db;
```

```bash
# 5. .env 填本機連線（DB_HOST=127.0.0.1、DB_USER=sa、DB_PASSWORD=上面設的密碼）

# 6. 建立資料表
cd myproject
python manage.py migrate

# 7. 匯入課程/使用者資料（關鍵！這步才會看到課程）
python manage.py loaddata fixtures/initial_data.json

# 8. 啟動
python manage.py runserver
```

開 http://127.0.0.1:8000/ 就會看到一樣的課程資料。

**測試帳號**（密碼見專案負責人）：管理員 `admin` / 老師 `teacher_python` / 學生 `student_amy`。

---

## 二、當你（負責人）更新了資料，要重新分享給同伴

每次你在自己電腦新增/修改了課程資料，重新匯出一次再 commit：

```bash
cd myproject
python -X utf8 manage.py dumpdata auth.user main --indent 2 --output fixtures/initial_data.json
git add fixtures/initial_data.json
git commit -m "更新課程資料"
git push
```

同伴那邊：

```bash
git pull
cd myproject
python manage.py loaddata fixtures/initial_data.json   # 會覆蓋更新成最新資料
```

---

## 三、上傳的影片與圖片

- 課程封面（`media/course_images/`）與示範影片（`media/course_videos/`）目前**有隨 git 一起同步**，所以同伴 clone 後就有。
- 若之後影片檔太大讓 repo 變肥，再改用雲端硬碟另外分享，並在 `.gitignore` 加入 `myproject/media/`。

---

## 四、注意事項

- 資料庫連線資訊一律放 `.env`（已被 git 忽略），不要寫死進 `settings.py`。
- 每個人的 `.env` 各自保留本機或共用雲端設定，不要互相覆蓋、不要 push `.env` 本身。
- 影片時長會在老師上傳影片時**自動偵測**，不需手動輸入。
